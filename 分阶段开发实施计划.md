# 一体化产品矩阵 · 分阶段开发实施计划

> 依据《一体化产品矩阵-产品开发与合规文档》拆解，面向**已有微信小程序「星眠AI」**的增量开发。
> 战略组合：① 安心眠（个人端·长期合规壁垒，单独推进）｜② 倦怠雷达（企业端·出收入）｜③ 能量驿站（个人端·养数据）。
> 闭环逻辑：个人养数据 → 匿名聚合 → 企业出收入 → 反哺个人/加固壁垒。

---

## 一、阶段总览与优先级

| 阶段 | 范围 | 优先级 | 依赖 | 上线前置（资质/备案） | 可上线状态 |
|---|---|---|---|---|---|
| **Phase 0 合规基座** | 用户协议、隐私政策、内容审核拦截、合规开关 | P0（最高） | 无 | **无资质即可上线** | ✅ 当前可发 |
| **Phase 1** | **③ 能量驿站 + ① 安心眠增强** | P1（核心） | 依赖 Phase 0 | 无资质（纯本地） | ✅ 当前可发 |
| **Phase 1.5（门控）** | 生成式 AI 睡眠助手 | P1（受限） | 依赖 Phase 0 | **必须算法备案完成** | ⛔ 备案前仅占位 |
| **Phase 2** | ② 倦怠雷达（企业端） | P2 | 依赖后端/云 + 员工单独同意 | 隐私政策发布 + 等保 + DPA | ⛔ 需立项 |
| **Phase 3** | 跨设备同步 / 数据汇聚 | P2 | 依赖 Phase 2 后端 | 隐私政策发布 + 等保 + 出境评估 | ⛔ 需立项 |

**核心结论**：Phase 0 + Phase 1 是**无资质即可合规上线**的部分，本次即开发完成；生成式 AI 与 ② 倦怠雷达 设门控/占位，待资质与备案到位后填充。

> **相位编号对照（与产品矩阵文档区分）**：本文档是**工程落地顺序**（按"合规可达性"排——先做完无资质即可上线的 ③+① 合规基座）；《一体化产品矩阵-产品开发与合规文档》§5 是**商业优先级路线图**（先 ② 倦怠雷达出收入）。两者的 Phase 编号不同属正常：战略先 ②、工程先 ③+①，并不矛盾。产品定位、竞品对标与差异化卡位详见矩阵文档 **§1.4 对标分析**。

---

## 二、Phase 1 详细设计（第 3 项 + 第 1 项）

### ▌第 3 项 · 能量驿站（个人端养数据）

**具体内容**
- 以「睡眠记录 + 作息打卡」为数据燃料，量化用户积累的**数据能量**与**数据资产**。
- 个人端可视化：能量总量、成长等级、获取账本（每次记录/打卡的明细）、数据资产概览（累计记录夜数、平均质量、达标率）。
- 预留「数据贡献换权益」扩展点（备案/许可后启用跨设备汇聚与权益兑换）。

**实现目标**
- 让用户在无后端情况下也能感知「我在积累有价值的健康数据」，形成留存与数据飞轮起点。
- 所有计算**本地纯函数**完成，零外部依赖；能量只增不减，可追溯。

**所需接口 / 数据**
| 数据/接口 | 来源 | 说明 |
|---|---|---|
| `sleep_records` | 本地 `wx.setStorageSync` | 既有睡眠记录，能量主燃料 |
| `routine_checkins` | 本地存储 | 作息打卡，准时额外加成 |
| 能量计算 | `utils/energy.js`（纯函数） | 无需后端 |
| 扩展点·云同步 | `config/compliance.js → BACKEND_SYNC.enabled` | 备案/等保后开启，对接 `cloudfunctions/syncSleep` |
| 扩展点·贡献换权益 | 同开关 | 资质齐备后展示兑换入口 |

**功能清单（本次交付）**
1. 能量总量 + 等级（萌芽/生长/茁壮/繁茂/森林）展示。
2. 能量账本：按日期列出每次获取（+12 记录/+4 高质量/+4 健康时长/+2 有备注；打卡 +6、准时 +6）。
3. 数据资产卡：累计记录夜数、平均质量、连续达标天数。
4. 底部扩展点提示：「跨设备同步 / 数据贡献」当前为本地模式，合规开放后启用（带明确说明，不虚假承诺）。

### ▌第 1 项 · 安心眠（个人端合规壁垒）

**具体内容**
- 现有 MVP 即为安心眠基座：睡眠记录、作息打卡、睡眠看板、助眠计时、隐私授权弹窗。
- 本次增强＝**把合规护城河做实**：补齐用户协议与隐私政策正文、内容审核拦截、统一合规开关，使产品在无医疗资质下仍合规运行、且为未来 SaMD 二类申报留好接口。

**实现目标**
- 产品定位严守「睡眠健康管理工具」，**绝不出现治疗/治愈/疗效等医疗断言**（由内容审核强制拦截）。
- 隐私与授权链路完整，可过审。

**所需接口 / 数据**
| 数据/接口 | 来源 | 说明 |
|---|---|---|
| 用户协议 / 隐私政策 | 新增 `pages/agreement`、`pages/privacy` | 静态正文页 |
| 内容审核 | `utils/audit.js` | 本地关键词拦截（笔记/反馈输入） |
| 合规开关 | `config/compliance.js` | 统一收口所有门控 |
| 隐私授权弹窗 | 既有 `wx.requirePrivacyAuthorize` 链路 | 需后台发布隐私协议后生效 |

**功能清单（本次交付）**
1. 用户协议页 + 隐私政策页（含数据收集范围声明：仅本机、不上传）。
2. 记录页「备注」保存前过内容审核，命中医疗断言/敏感词则拦截并提示。
3. 首页底部协议/隐私入口，与隐私授权弹窗形成完整链路。
4. 合规开关集中管理，便于后续 SaMD 申报、算法备案后逐项开启。

---

## 三、合规保护机制（无资质先落地）

> 以下四项**不依赖任何许可资质，当前即可上线**，是「先合规、后扩展」的基座。

1. **用户协议（`pages/agreement`）**：明确产品性质（非医疗、非诊疗）、数据仅本机、免责条款。
2. **隐私政策（`pages/privacy`）**：声明收集字段、用途、存储位置（本机）、用户权利（导出/清空/撤回），并与后台《隐私保护指引》呼应。
3. **内容审核拦截（`utils/audit.js`）**：本地关键词拦截，覆盖医疗断言（治疗/治愈/根治/疗效/处方/诊断/疗程…）与引流敏感词，输入即拦，无需服务端。
4. **合规开关（`config/compliance.js`）**：集中定义 `AI.enabled / BACKEND_SYNC.enabled / CONTENT_AUDIT` 等，所有受约束功能统一从此读取，**预留扩展点**清晰可见。

**扩展点预留方式**
- 每个受限功能在代码中保留完整调用位，仅由开关置灰/占位。
- 开关旁写明「前置条件」与「上线范围」，运营/法务可按 Checklist 逐项解锁，无需重构。

---

## 四、生成式 AI 阶段（算法备案后方可接入）

> 当前**所有生成式 AI 能力一律关闭**，仅保留入口与说明（gated placeholder）。

**前置条件（全部满足方可开启 `AI.enabled=true`）**
1. ✅ 完成**生成式人工智能服务算法备案**（网信办）。
2. ✅ 完成**个人信息保护影响评估（PIPIA）**。
3. ✅ 接入**内容安全审核**（涉政/暴恐/违禁/医疗断言），AI 输出同样过 `utils/audit.js` 及服务端审核。
4. ✅ 医疗相关输出加**人工复核或显著免责声明**，且产品仍持「非诊疗」定位。

**上线范围（备案后）**
- 仅开放经备案的对话/建议类型（如 CBT-I 科普、睡眠卫生提示）。
- 禁止输出诊断、用药、治疗处方；所有 AI 文案经审核与免责标注。
- 仍受 `BACKEND_SYNC` 约束：AI 上下文如需跨会话，须等数据合规通道就绪。

**当前实现**：`pages/ai` 展示上述前置条件与范围说明，默认不可进入生成流程，点击即提示「待算法备案完成后合规上线」。

---

## 五、依赖关系与排期

```
Phase 0 合规基座 ──► Phase 1 ③能量驿站 + ①安心眠（本次完成）
                         │
                         ├─► Phase 1.5 生成式AI（门控，备案后开）
                         │
Phase 1 完成 ──────────► Phase 2 ②倦怠雷达（需后端+员工单独同意）
                         │
                         └─► Phase 3 跨设备同步/数据汇聚（需等保+出境评估）
```

**排期建议**
- **本周（本次）**：Phase 0 + Phase 1 全部代码完成并过校验，可提交审核版本。
- **算法备案周期（约 1–3 月）**：并行准备 PIPIA 与内容安全接入，备案完成即解锁 Phase 1.5。
- **立项后**：Phase 2/3 需后端/云开发、企业客户签约、等保测评，单独排期。

---

## 六、本次（Phase 0 + Phase 1）交付清单

- [x] `config/compliance.js` 合规与功能开关
- [x] `utils/audit.js` 内容审核（本地关键词拦截）
- [x] `utils/energy.js` 能量计算（纯函数）
- [x] `pages/agreement` 用户协议页
- [x] `pages/privacy` 隐私政策页
- [x] `pages/energy` 能量驿站页
- [x] `pages/ai` 生成式 AI 占位页（门控）
- [x] 首页改造：能量卡片 / AI 入口 / 协议链接 / 助眠改子页进入
- [x] `record.js` 接入内容审核；`record.js`+`routine.js` 触发能量重算
- [x] `app.json` 注册新页面 + tabBar 替换为「能量驿站」
- [x] 全量语法/JSON 校验通过

> ⚠️ 法律结论为梳理与 Checklist，非法律意见书；正式申报/上线前请过执业律师。
